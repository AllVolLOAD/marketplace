type TrackingRiderProps = {
  id?: string;
  onLocationUpdate?: (location: { lat: number; lng: number }) => void;
};

const TrackingRider = (props: TrackingRiderProps) => {
  void props;
  return null;
};

export default TrackingRider;
